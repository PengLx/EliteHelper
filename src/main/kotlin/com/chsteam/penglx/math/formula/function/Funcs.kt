package com.chsteam.penglx.math.formula.function

/**
 * RPGCore © 2022
 */
enum class Funcs(val func: Func) {
    ABS(Abs),
    CEIL(Ceil),
    COS(Cos),
    FLOOR(Floor),
    LOG(Log),
    RAND(Rand),
    RAND_INT(RandInt),
    ROUND(Round),
    SIGN(Sign),
    SIN(Sin),
    SQ(Sq),
    SQRT(Sqrt),
    TAN(Tan),
    TRAND(TRand),
    TRAND_INT(TRandInt)
}